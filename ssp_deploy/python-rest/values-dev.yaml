global:
  sharedVolumeSpringConfiguration:
    properties:
      storageSize: "5Gi" # storage size in Persistent Volume Claim (PVC)

springboot:
  image: python
  imageTag: v1
  port: 5000
  strategy: Recreate
